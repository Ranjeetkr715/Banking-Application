server:
  port: 9094

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_details
    username: root
    password: Ranjeet@97
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    user:
      name: admin
      password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    org.springdoc: DEBUG